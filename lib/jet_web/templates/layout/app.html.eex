<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Jet · Phoenix Framework</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@100;400;700;900&display=swap" rel="stylesheet">
    <!--<link href="<%= Routes.static_path(@conn, "/css/pattern.css") %>" rel="stylesheet"> -->
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
  <div id="header" class="bg-green-600 py-3">
    <div class="w-10/12 m-auto">
      <%= link(to: Routes.page_path(@conn, :index), class: "flex inline-block") do %>
      <img src="<%= Routes.static_path(@conn, "/images/logo.png") %>" alt="Jet logo"  style="width:40px; height:35px;"><span class="text-white font-bold ml-4 mt-2">HTTP Jet</span>
      <% end %>
    </div>
  </div>

    <main role="main">
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      <%= @inner_content %>
    </main>
  </body>
</html>
